<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Project_MyShop_2025.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_MyShop_2025.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#F8F9FA">

    <ScrollViewer>
        <Grid Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Reports &amp; Statistics" FontSize="32" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Text="Business insights and analytics" FontSize="14" Foreground="#666666" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Filter Controls -->
            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Time Period:" FontSize="14" FontWeight="SemiBold" Foreground="#1A1A1A" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    
                    <StackPanel Grid.Column="1" Spacing="4">
                        <TextBlock Text="From Date" FontSize="11" Foreground="#666666"/>
                        <CalendarDatePicker x:Name="FromDatePicker" DateChanged="DateFilter_Changed"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Spacing="4">
                        <TextBlock Text="To Date" FontSize="11" Foreground="#666666"/>
                        <CalendarDatePicker x:Name="ToDatePicker" DateChanged="DateFilter_Changed"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Period Selector -->
            <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Group By:" FontSize="14" FontWeight="SemiBold" Foreground="#1A1A1A" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <ComboBox x:Name="PeriodComboBox" Grid.Column="1" SelectionChanged="PeriodComboBox_SelectionChanged">
                        <ComboBoxItem Content="Daily" Tag="Day" IsSelected="True"/>
                        <ComboBoxItem Content="Weekly" Tag="Week"/>
                        <ComboBoxItem Content="Monthly" Tag="Month"/>
                        <ComboBoxItem Content="Yearly" Tag="Year"/>
                    </ComboBox>
                </Grid>
            </Border>

            <!-- Charts Section -->
            <StackPanel Grid.Row="3" Spacing="16">
                <!-- Product Sales Chart (Line Chart) -->
                <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel Spacing="20">
                        <StackPanel Spacing="4">
                            <TextBlock Text="📊 Product Sales" FontSize="18" FontWeight="SemiBold" Foreground="#1A1A1A"/>
                            <TextBlock Text="Product sales quantity over time" FontSize="12" Foreground="#999999"/>
                        </StackPanel>
                        
                        <Grid Height="300" Margin="0,8,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Background="#FAFAFA" CornerRadius="8">
                                <Canvas x:Name="ProductSalesChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                            
                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                                <StackPanel x:Name="ProductSalesLabels" Orientation="Horizontal"/>
                            </ScrollViewer>
                        </Grid>
                        
                        <Border Background="#F5F5F5" CornerRadius="8" Padding="16,12">
                            <TextBlock x:Name="ProductSalesSummaryText" Text="Loading chart data..." FontSize="12" Foreground="#666666" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Revenue & Profit Chart (Bar Chart) -->
                <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel Spacing="20">
                        <StackPanel Spacing="4">
                            <TextBlock Text="💰 Revenue &amp; Profit" FontSize="18" FontWeight="SemiBold" Foreground="#1A1A1A"/>
                            <TextBlock Text="Revenue and profit comparison" FontSize="12" Foreground="#999999"/>
                        </StackPanel>
                        
                        <Grid Height="300" Margin="0,8,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Background="#FAFAFA" CornerRadius="8">
                                <Canvas x:Name="RevenueProfitChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                            
                            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,12,0,0">
                                <StackPanel x:Name="RevenueProfitLabels" Orientation="Horizontal"/>
                            </ScrollViewer>
                        </Grid>
                        
                        <Border Background="#F5F5F5" CornerRadius="8" Padding="16,12">
                            <TextBlock x:Name="RevenueProfitSummaryText" Text="Loading chart data..." FontSize="12" Foreground="#666666" HorizontalAlignment="Center"/>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>

